# Content & Feature Index

Tracks test/demo pages authored in da.live and the block features they exercise. Update this whenever a new test page or block variant is added.

| Path | Purpose | Block(s) | Status | PR |
| --- | --- | --- | --- | --- |
| Site-wide (`styles/styles.css`) | Day 1: updated global color tokens (`--background-color`, `--text-color`, `--link-hover-color`) | — | Live on main | [#1](https://github.com/Shekhar-sj/ssj-eds-da/pull/1) |
| Site-wide (`styles/styles.css`) | **2026-09-07:** replaced the Day 1 cyan/magenta/red tokens with the accessible **Soft neutral** palette — `--background-color` `cyan`→`#fafafa`, `--text-color` `magenta`→`#1f2933`, `--link-color` `#3b63fb`→`#2557d6`, `--link-hover-color` `red`→`#1b3fa0`, and nudged `--light-color` `#f8f8f8`→`#eef1f5` so highlight sections stay distinct against the new background. Fixes the low contrast (magenta-on-cyan) that was pulling down the Lighthouse A11Y score. | — | In PR | — |
| `/banner-test` | Day 2: Banner block — image + title over full-width backdrop, background color optional (row 3), defaults to blue. Covers default color, custom color, and the `Banner (dark)` variant (with/without custom color); each instance authored in its own section for correct spacing. | `banner` | Live on main, CMS-verified | [#2](https://github.com/Shekhar-sj/ssj-eds-da/pull/2), [#3](https://github.com/Shekhar-sj/ssj-eds-da/pull/3) |
| `library/blocks/hero` | Day 2: registered `Hero` in the da.live block Library — `library/blocks` sheet (name/path index) + `library/blocks/hero` doc (working example) + `library` tab in the site `config` sheet pointing at `library/blocks.json`. da.live-only, no repo code involved. Confirmed: Hero appears in the Library panel when authoring `banner-test`. | `hero` | CMS-verified | [#4](https://github.com/Shekhar-sj/ssj-eds-da/pull/4) |
| `/` (homepage) | Day 2: added a separate, third section with a `Section Metadata` (`Style: highlight`) table, giving it the existing light-grey (`--light-color`) background — visually distinct from the cyan default. da.live-only, no repo code involved (uses existing `main .section.highlight` rule in `styles/styles.css`). | — | Live on main | [#5](https://github.com/Shekhar-sj/ssj-eds-da/pull/5) |
| `/banner-test`, `library/blocks/hero` | Day 2: ported the [WKND breadcrumb block](https://github.com/hlxsites/wknd/tree/main/blocks/breadcrumb), adapted to build the trail from the real URL path (`Home > ...ancestors... > page title`) instead of WKND's hardcoded "Adventures" crumb, and restyled with this project's own CSS custom properties. Zero-config: authors place an empty `Breadcrumb` block anywhere. Verified at both a 1-level path (`Home > Banner Assignment`) and a 3-level path (`Home > Library > Blocks > Hero Page`) — required fixing each page's `Metadata` `Title` row, which had been mistakenly set to "Breadcrumb". | `breadcrumb` | Live on main | [#5](https://github.com/Shekhar-sj/ssj-eds-da/pull/5) |
| `/banner-test` | Day 2: adapted the [Block Party Note/Alert block](https://github.com/kronnox/extra-block-collection/tree/main/blocks/note) — callout box with `info`/`warning`/`error`/`success` variants, `accent-border` and `no-icon` options. Remapped its CSS custom properties to this project's own tokens, added 4 new shape-distinct icon SVGs (`icons/info.svg` etc. — the source relied on color alone, this project's icons differ by shape too for accessibility). Verified on the feature-branch preview with default/warning/success variants, including repeated instances of the same variant. | `note` | Live on main | [#6](https://github.com/Shekhar-sj/ssj-eds-da/pull/6) |
| `/embed-block` | Day 2: added the [Block Collection `embed` block](https://www.aem.live/developer/block-collection/embed) (Adobe-vetted, used mostly as-is) — lazy-loads a YouTube video or X/Twitter post from a plain link, with an `aria-label` added to the play-button placeholder (source had none) and `x.com` added alongside `twitter.com` for domain matching. Verified on the feature-branch **live** URL (not just preview, per this exercise's acceptance criteria): YouTube video plays, X post renders via Twitter's widget script; unrelated YouTube ad-conversion CORS console noise confirmed not caused by this block. | `embed` | Live on main | [#7](https://github.com/Shekhar-sj/ssj-eds-da/pull/7) |
| `/fragments/teaser`, `/banner-test` | Day 2: reusable teaser fragment — plain content (image, text, "Learn More" button link), no block wrapper inside the fragment doc itself (that was the initial bug: authors must NOT wrap a fragment's own content in a `Fragment` table, only the *host* page's `Fragment` block should reference the fragment's path). Embedded on `banner-test` via two `Fragment` block instances (plain-text path and linked path, both valid per `fragment.js`). Required publishing the fragment doc itself, not just previewing it — `fragment.js` fetches relative to whichever environment (preview/live) the host page is on. da.live-only, no repo code involved (uses the existing, unmodified `fragment` block). | `fragment` | Live on main | — |
| `/about`, `/contact`, `/nav` | Day 2: created two new pages and added them to the header nav as plain top-level links (no nested list, so `header.js` doesn't treat them as dropdowns). da.live-only, no repo code involved (uses existing, unmodified `header` block). Required publishing `/nav` itself, not just the pages that use it — same "fragment-like" gotcha as the teaser fragment, since `header.js` also fetches its content relative to whichever environment is currently loaded. | `header` | Live on main | — |
| `/terms`, `/footer` | Day 2 (final task): created a Terms & Conditions page and repointed the footer's existing "Terms of Use" link to it (`/terms`) instead of adding a duplicate link — cleaner than adding a second link. da.live-only, no repo code involved (uses existing, unmodified `footer` block). Same publish-independently gotcha as `/nav`/fragments applies to `/footer`. Shared footer means the link shows on every page automatically. | `footer` | Live on main | — |
| `/content-types` | Day 3: single page demonstrating all 6 EDS content types in one place — Text (heading + paragraph), Image (`<picture>`), Video (`Embed` block, YouTube), Links (inline link), Section (3 section breaks), Block (`Banner` instance). Reused existing blocks rather than creating anything new. Verified live. | `embed`, `banner` | Live on main | — |
| `/tobedelected` | Day 3 (#3, INCOMPLETE): unpublish-then-delete exercise. Source deleted from da.live, but live+preview copies are orphaned (still `200`) — this sandbox org has no authorization configured, so unpublish is role-gated; anonymous `DELETE /live` returns 403 and the da.live UI unpublish fails. Lesson: **always unpublish before deleting the source** (deleting source never unpublishes). Parked — needs the `publish` role set up via Config Service (`auth` skill → POST `access/admin.json`) to clear the orphaned copies. | — | Blocked (role setup needed) | — |
| `/metadata-demo` | Day 3 (#5): per-page **Metadata block** demonstrating all six fields — Title, Description, Image, Theme, Template, Tags. Verified in `<head>`: Title→`<title>`/`og:title`/`twitter:title`; Description→`description`/`og:description`/`twitter:description`; Image→`og:image`(+`:secure_url`)/`twitter:image` (an *inserted* image, not a text URL, so EDS serves the optimized `media_…jpg`); Theme→`name="theme"`; Template→`name="template"`; **Tags→`property="article:tag"` (one per comma-split value)** — note `Tags` is a special reserved key, distinct from free-form `Keywords`→`name="keywords"`. Block sits at the page bottom and is stripped from the body. da.live-only, no repo code. | `metadata` | Live on main | — |
| `/redirects` (sheet), `/legacy/home` | Day 3 (#4): created a `redirects` sheet (columns `Source`/`Destination`, row `/legacy/home` → `/`). Verified `/legacy/home` returns `301 Moved Permanently` with `location: /` on both live and preview. da.live-only, no repo code. Same publish-independently gotcha (sheet must be published to take effect). Note: only a tab named `helix-default` executes redirects if multiple tabs exist; single default tab is fine. | — | Live on main | — |
| [`/metadata`](https://da.live/sheet#/shekhar-sj/ssj-eds-da/metadata) (sheet) | Day 3 (#6): site-wide **bulk-metadata sheet** — `URL`-pattern rows apply metadata to many pages at once; every non-`URL` column header becomes a meta tag (`og:*`/`twitter:*` emitted verbatim). `/**` row = site-wide defaults (Title, Description, Image, Theme, Template, Tags, og:*, twitter:*); `/library/**` row demonstrates precedence — the more-specific row overrides, empty cells fall back to `/**`. A page's own `<title>`/Metadata block still wins over the sheet. Image referenced by uploaded asset path (sheets are text-only — can't insert images); `/default-danger-share.jpg` 301→`media_…jpg`→200 (social scrapers follow the redirect). Verified on `/about` (sheet-only) and `/library/blocks/hero` (both rows). da.live-only, no repo code. Same publish-independently gotcha (sheet must be published). | `metadata` (sheet) | Live on main | — |
| `/employee-directory` + `/employees` & `/placeholders` (sheets) | Day 3 (#7): **`employee-list` block** (new code) — reads an employees sheet (`Name`, `Department`, `Experience`, `City`) published as JSON, renders 10 rows at a time in a responsive table (stacked cards on mobile), with a **Load more** button that appends the next 10 and self-removes when exhausted. Button label sourced from the `placeholders` sheet. **Gotcha:** EDS `toCamelCase` lowercases first, so the placeholder Key must be `load-more` (→ `loadMore`), NOT `loadMore` (→ `loadmore`, won't match). Header shows `Experience (In years)` via a separate display label while the data key stays `Experience`. Source path read from the block's cell (defaults to `/employees.json`). Self-contained `fetchPlaceholders()` since this boilerplate's `aem.js` doesn't export one (aem.js never modified). Lint clean; 9/9 jsdom DOM tests pass (10→20→25, button removed). Verified on the feature-branch live env against real da.live sheets (25 rows → two Load-more clicks). | `employee-list` | In PR | [#9](https://github.com/Shekhar-sj/ssj-eds-da/pull/9) |
| `/ew-demo` | Day 3 (#8): enabled **Experience Workspace** via the guided setup — Step 1 set the `ew.enabled=true` flag in the site `flags` sheet (`da.live/config#/shekhar-sj/ssj-eds-da`). Authored `/ew-demo` in the EW canvas editor and published (live+preview `200`; inherits the bulk-metadata `<title>`). **Step 2 (Sidekick `editUrlPattern`) parked** — the guided tool couldn't write it (no EDS config-admin role in this sandbox, same gate as #3); EW authoring works via da.live directly without it. da.live-only, no repo code. | — | Live on main | — |

## Columns

- **Path**: da.live document path (same path on `.aem.page` / `.aem.live`)
- **Purpose**: what the page demonstrates and any notable edge cases covered
- **Block(s)**: block name(s) used on the page
- **Status**: `Draft` (local/da.live only, not merged), `In PR` (link included), `Live on main`
- **PR**: link to the PR that introduced/last touched the related code, if any
